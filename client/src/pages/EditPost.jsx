import {useAuth} from "../context/AuthContext";

export default function EditPost(){
    

    return <main>
        <form>
    <label>
        “Edit Post”.
    </label>

        </form>
    </main>
}